#include <stdio.h>
# define PI 3.14


void main ()
{
        int radius = 2;
        int height = 4;
        float area = 2 * PI * radius * height + 2 * PI * radius * radius; 
        printf("%f", area);
        return;
}        
